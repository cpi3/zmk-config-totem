# Keep the BLE peripheral awake. Deep-sleeping a split peripheral can leave
# the USB dongle central stuck waiting for the link to recover.
CONFIG_ZMK_SLEEP=n
CONFIG_ZMK_IDLE_TIMEOUT=30000

CONFIG_ZMK_BLE_CLEAR_BONDS_ON_START=n

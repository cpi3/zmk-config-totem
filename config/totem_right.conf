# =====================================================================
# config/totem_right.conf
# Personal overrides for the "totem_right" shield ONLY.
# Same reasoning as totem_left.conf — battery powered, real keys,
# normal deep sleep is fine.
# =====================================================================

CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=600000
CONFIG_ZMK_IDLE_TIMEOUT=30000

CONFIG_ZMK_BLE_CLEAR_BONDS_ON_START=n
